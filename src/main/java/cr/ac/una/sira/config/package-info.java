/**
 * Configuracion transversal de Spring. Vacio en el Lab 1: aca van los
 * datasources (Lab 2), Spring Security con los roles PROFESIONAL y ENCARGADO
 * (Lab 5) y el CORS para el frontend React (Lab 6).
 *
 * No es una capa de la arquitectura, por eso no entra en la regla
 * presentation -> business -> data.
 */
package cr.ac.una.sira.config;
